import { ValidatePreventDirective } from './validate-prevent.directive';

describe('ValidatePreventDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePreventDirective();
    expect(directive).toBeTruthy();
  });
});
